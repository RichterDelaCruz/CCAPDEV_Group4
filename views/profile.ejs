<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/board.css">
    <link rel="stylesheet" href="/css/homepage.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
</head>

<body>
    <nav>
        <div class="search-box">
       
       <a href="/homepage">Homepage</a>
       <a href="/users">Search Users</a>
       <a href="/login">Log out</a>
       
        </div>
    </nav>

    <div class="board-container">
    <div class="board-contents" style="cursor: pointer;">
    

    <div class="board-profile">
    <!-- Display the profile picture if it exists -->
    <div class="board-profile-name">
    <% if (user.profilePicture) { %>
        <img src="<%= user.profilePicture %>" alt="Profile Picture" style="max-width: 100px;">
        <% } else { %>
            <!-- Display a default profile picture if the user has not uploaded a custom picture -->
            <img src="/images/default-profile-picture.jpg" alt="Default Profile Picture" style="max-width: 100px;">
            <p>No profile picture uploaded.</p>
            <% } %>
        <span>@<%= user.username %></span>
    </div>
    </div>
    
    
<div class="board-photos">
<% posts.forEach((post) => { %>
        <div>
            <a href="/post/<%= post._id %>">
                <% if (post.photo) { %>
                    <img src="<%= post.photo %>" alt="Post Photo">
                <% } %>
            </a>
        </div>
    <% }); %>
    </div>
    </div>
    </div>
</body>
</html>